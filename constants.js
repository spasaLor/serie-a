// Dimensioni del sistema di coordinate SVG della mappa (viewBox).
export const MAP_WIDTH = 760;
export const MAP_HEIGHT = 880;

// Quanto "esplodere" le regioni verso l'esterno rispetto al centro mappa,
// per dare respiro visivo tra una regione e l'altra.
export const EXPLODE_FACTOR = 0.09;
